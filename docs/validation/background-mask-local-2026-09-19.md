# 背景与多目标遮罩本地验收 — 2026-09-19

## 自动化验证

- 后端相关测试：123 passed，包括七种非空组合、独立背景推理、部分失败、
  空前景/空背景、非正方形坐标还原、边缘参数、位图命名空间、合并交换律、
  旧提案兼容及事务应用/替换/撤销。
- 下载临时目录加强后重跑背景模块：13 passed；覆盖校验失败不发布、校验成功
  才原子替换正式文件，以及同尺寸内容损坏时的强制校验。
- 前端全量：134 files / 1226 tests passed，包含本次多选、模型依赖、分组选择、
  失败筛选、参数变化、任务隔离、像素并集及应用保护测试。
- `npm run build`（ESLint、TypeScript、Vite）通过；保留原有大 chunk 提示。
- 最后调整空目标状态文案后，面板 17 项测试及完整构建再次通过。
- 新后端模块及测试 Ruff 通过，`git diff --check` 通过。
- `tools/check_distribution_features.py`：auto_head_mask=3，更新保护存在。

前端首次全量运行曾出现另外一项并行标签排序改动的三个失败用例；该改动修复后
重跑全量得到上述通过结果。本次保留了工作区中那项改动。

## 实际模型效果（与浏览器验收分开记录）

本地 ONNX Runtime 1.24.4，实际使用 DirectML。固定的背景模型为
`skytnt/anime-seg@493cb60893f47441b26ec4fb9a306bce9e342982/isnetis.onnx`，
大小 176,069,933 bytes，SHA-256：
`f15622d853e8260172812b657053460e20806f04b9e05147d49af7bed31a6e99`。
模型直接推理，没有创建转换环境。

前三张素材取自作者演示空间
[anime-remove-background/examples](https://huggingface.co/spaces/skytnt/anime-remove-background/tree/main/examples)。
另生成一张纯色无人物图，并裁出第三张的天空区域作为第二张无人物图。
默认参数为人物阈值 0.5、保护边距 0、羽化 0。

| 样本 | 尺寸 | 实际结果 |
| --- | --- | --- |
| 01：单人、发丝、室内背景 | 512 × 714 | 1 脸部 + 1 背景；背景 48.1% |
| 02：单人、遮挡、复杂色彩 | 512 × 853 | 1 脸部 + 1 背景；背景 42.2% |
| 03：四人、烟花背景、宽图 | 1967 × 1360 | 4 脸部 + 1 背景；背景 41.6% |
| 04：纯色无人物 | 512 × 384 | 无人物，不生成背景提案 |
| 05：天空裁剪 | 240 × 220 | 无人物，不生成背景提案 |

对输出预览进行目视检查：脸部与背景同时覆盖，四个人物的衣服、头发均保留，
人物间及肢体间背景可被遮罩。细发丝边缘属于模型预测，未声称逐像素标注级准确。
原始图片、结果 JSON、灰度权重及合并预览保存在本地忽略目录 `tmp/background-qa/`。
真实 CPU/CUDA 推理未在本轮单独验收；CPU 回退沿用现有机制及测试。

## 真实浏览器验收

使用构建后的本地服务 `http://127.0.0.1:8765`，在临时样本项目
Background mask QA（project 3 / version 8）执行，未应用到用户训练数据。

- 训练遮罩默认仅选脸部；勾选背景后提示旧提案需重检测，并禁用应用。
- 取消全部目标后检测被禁用；仅背景检测当前图成功，任务元数据只记录
  `background_segmenter`。折叠参数区仅显示三项背景参数，默认值为 0.5 / 0 / 0。
- 检测全部得到五张图、九个区域；成功提案默认选中，无人物两图可按背景状态筛选。
- 单人、多人、复杂背景/遮挡样本的实际位图预览与模型输出一致。
- 分别取消背景及脸部提案后，对应预览立即消失，另一目标保持显示。
- 一次应用写入三张图片，出现遮罩标记；一次撤销恢复三张图片应用前状态。
- 重新检测后安全替换旧组合记录。取消样本 02 的脸部时，差异预览显示恢复学习
  9,322 像素、新增忽略 0 像素；确认后只改动该图片，撤销可恢复旧组合遮罩。
- 应用后可画手工笔画；未保存笔画存在时再次应用被阻止并明确提示先保存。
  测试笔画随后撤回。
- 在 1280 × 720 和较窄的 886 × 1096 页面中检查右栏滚动，分组、应用及替换操作可达。
  较窄页面收起左导航后，画布可用空间恢复；未改动原有三栏总体布局。

验收后将临时项目归档（可恢复，样本与操作记录保留），本地服务重新加载最终代码。

上述为有限样本本地验收，不代表所有画风、道具或细发丝都能正确分割。人工审核
和画笔/橡皮修补仍是操作流程的一部分。没有启动训练或修改训练配置。
